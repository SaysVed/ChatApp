import jwt from 'jsonwebtoken';
import UserAccount from '../Models/RegisterModel';
import dotenv from 'dotenv';
dotenv.config();

const verifyLogin = async (req, res, next) => {
    
};

export default verifyLogin;
